const logItems = function (items) {
  for (const item of items) {
    console.log(item);
  }
};

console.log(logItems(['Ihor', 'Britny', 'Lisa', 'Bart']));
logItems(['Ihor', 'Britny', 'Lisa', 'Bart']);
